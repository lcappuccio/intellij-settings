/**
 * @author ${USER}
 * @date ${DATE} ${TIME}
 */
#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};

#end
import org.aspectj.lang.annotation.Aspect;

@Aspect
public class ${NAME} {
}
